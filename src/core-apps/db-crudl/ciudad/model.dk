#include "dkli.dkh"
#!

module "edoprov.dk"
{
    @list_query="select #<@sys_fields>, codigo,codigotel,nombre,estado from ciudad where estado=@edo order by codigo;"
    @read_query="select #<@sys_fields>, codigo,codigotel,nombre,estado from #<@table_name> where #<@keyfield>=@_entity_id limit 1;"
    // do rise_error(0,ftext(@read_query))

    // ref @delete=@null
}